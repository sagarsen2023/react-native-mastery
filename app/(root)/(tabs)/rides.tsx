import { Text, View } from 'react-native'
import React from 'react'
import { SafeAreaView } from 'react-native-safe-area-context'

const Rides = () => {
  return (
    <SafeAreaView>
      <View>
        <Text className='text`-2xl'>Uber Clone All rides</Text>
      </View>
    </SafeAreaView>
  )
}

export default Rides
