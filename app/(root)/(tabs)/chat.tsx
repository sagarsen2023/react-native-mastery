import { Text, View } from 'react-native'
import React from 'react'
import { SafeAreaView } from 'react-native-safe-area-context'

const Chat = () => {
  return (
    <SafeAreaView>
      <View>
        <Text className='text`-2xl'>Uber Clone Chat Page</Text>
      </View>
    </SafeAreaView>
  )
}

export default Chat
